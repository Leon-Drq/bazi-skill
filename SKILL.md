---
name: bazi-mingli
description: Build and interpret a BaZi Four Pillars chart for natal structure, career, wealth, relationships, study, daily/monthly/yearly themes, or major-luck cycles. Use for 八字, 四柱, 五行, 十神, 日主, 命书, 大运, 流年, 年运, or 月运; requires verified birth-time conventions.
---

# 八字四柱

这个 skill 将出生时间转换为年、月、日、时四柱，并以日主为中心输出五行、十神、身强身弱的可复核线索。它适合文化学习、自我观察和问题结构化，不替代医疗、法律、投资、职业或关系专业意见。

## 输入门控

尽量收集：

- 公历出生日期和具体时间；
- 性别，用于大运顺逆；
- 出生地或 IANA 时区，至少说明采用的时区；
- 是否采用真太阳时、子初换日等流派选项。

如果用户给的是农历，先要求确认闰月和对应公历日期；如果出生时间跨越节气、立春或 23:00 子时，必须标注边界不确定性。月柱以节气月而不是农历初一切换。

## 固定流程

1. 记录历法、时区、换日和节气边界假设；
2. 运行 `scripts/bazi_calc.py` 生成四柱、藏干、十神、五行统计和大运骨架；
3. 读取 `references/four-pillars.md` 核对天干地支、五行生克和节气月；
4. 读取 `references/reading-rules.md`，先判断日主强弱和证据，再讨论用神、主题和时间段；
5. 专题分析只引用盘面字段：感情看财官、夫妻宫/合冲等实际字段；事业看官杀、印、食伤和大运；健康只输出生活方式观察，不能诊断。

## 时间模式

- 原局：只讨论稳定结构，不夹带某一年的事件预测；
- 大运：原局 + 当前大运，说明十年主题与转折条件；
- 年运：原局 + 大运 + 指定流年，按季度/月令说明窗口；
- 月运：原局 + 大运 + 流年 + 指定流月，避免伪造精确日期；
- 今日提示：只做轻量行动提醒，不用一天的干支覆盖原局全部结构。

同一人的时间分析必须复用同一份经核验命盘和历法选项。

## 排盘边界

- 四柱、十神和五行是计算层；“性格、事业、婚姻、财运”是解释层，不能混写成事实；
- 身强/身弱是简化评分，不等于完整格局、调候或盲派结论；
- 大运起运岁数需要精确节气时刻，脚本只用节气日期近似，必须标注“近似”；
- 缺少时辰时可排前三柱，但不可伪造时柱；
- 流年分析必须明确年份，并同时查看原局、大运和流年的组合。

## 输出契约

固定输出：

1. 输入与历法假设；
2. 四柱表：干支、五行、阴阳、藏干、十神；
3. 五行统计和日主强弱证据；
4. 喜用/忌避的暂定方向及依据；
5. 大运顺逆、起运近似和运柱；
6. 主题结论、现实行动和不确定性；
7. 文化参考边界说明。

避免“注定、必然、一定发财/生病/离婚”等断言。遇到现实高风险问题，先给现实核查和专业建议。

## 脚本

```bash
python3 scripts/bazi_calc.py 1990 8 15 14 男
```

脚本默认按公历、东八区、约定的节气日期和 23:00 子时边界生成 JSON。需要严谨历法时，先用专业万年历核对，再把核对后的四柱作为输入分析。报告遵守 `references/evidence-contract.md`；医疗、财务、婚姻和职业重大决定同时遵守 `references/safety-and-privacy.md`。
